import React from 'react'
import FetchContent from './FetchContent'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <FetchContent />
    </div>
  )
}

export default page
